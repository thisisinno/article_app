"use client";
import {createContext,useContext,useEffect,useState} from "react";import {api} from "@/lib/api";import type{User}from"@/lib/types";
type Ctx={user:User|null;authOpen:boolean;composerOpen:boolean;openAuth:()=>void;openComposer:()=>void;close:()=>void;refresh:()=>void};const Context=createContext<Ctx|null>(null);
export function useApp(){const v=useContext(Context);if(!v)throw Error("Missing AppProvider");return v}
export function AppProvider({children}:{children:React.ReactNode}){const[user,setUser]=useState<User|null>(null),[authOpen,setAuth]=useState(false),[composerOpen,setComposer]=useState(false);const refresh=()=>api<{user:User|null}>("/auth/me/").then(x=>setUser(x.user)).catch(()=>{});useEffect(()=>{void refresh()},[]);const close=()=>{setAuth(false);setComposer(false)};return <Context.Provider value={{user,authOpen,composerOpen,openAuth:()=>setAuth(true),openComposer:()=>user?setComposer(true):setAuth(true),close,refresh}}>{children}<AuthDialog/><ComposerDialog/></Context.Provider>}
function AuthDialog(){const{authOpen,close,refresh}=useApp();const[mode,setMode]=useState<"login"|"register">("login"),[error,setError]=useState("");if(!authOpen)return null;async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));try{await api(`/auth/${mode}/`,{method:"POST",body:JSON.stringify(d)});refresh();close()}catch(x){setError((x as Error).message)}}return <div className="overlay" onMouseDown={e=>e.target===e.currentTarget&&close()}><dialog open aria-modal="true" aria-labelledby="auth-title"><button className="dialogClose" onClick={close} aria-label="Close">×</button><h2 id="auth-title">{mode==="login"?"Welcome back":"Join Insight"}</h2><p>Continue the conversation without losing your place.</p><form onSubmit={submit}>{mode==="register"&&<><label>Display name<input name="display_name" required/></label><label>Email<input name="email" type="email" required/></label></>}<label>Username<input name="username" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label>{error&&<p className="formError">{error}</p>}<button className="primary">{mode==="login"?"Sign in":"Create account"}</button></form><button className="textButton" onClick={()=>setMode(mode==="login"?"register":"login")}>{mode==="login"?"Create an account":"I already have an account"}</button></dialog></div>}
function ComposerDialog(){const{composerOpen,close}=useApp();const[body,setBody]=useState(""),[type,setType]=useState("short"),[title,setTitle]=useState("");useEffect(()=>{const v=localStorage.getItem("insight-draft");if(v)setBody(v)},[]);useEffect(()=>{localStorage.setItem("insight-draft",body)},[body]);if(!composerOpen)return null;async function publish(){await api("/posts/",{method:"POST",body:JSON.stringify({body,title,type})});setBody("");localStorage.removeItem("insight-draft");close();location.reload()}return <div className="overlay"><dialog open aria-modal="true"><button className="dialogClose" onClick={close}>×</button><h2>Create</h2><div className="typeSwitch"><button className={type==="short"?"active":""} onClick={()=>setType("short")}>Post</button><button className={type==="article"?"active":""} onClick={()=>setType("article")}>Article</button></div>{type==="article"&&<input className="titleInput" value={title} onChange={e=>setTitle(e.target.value)} placeholder="Article title"/>}<textarea autoFocus value={body} onChange={e=>setBody(e.target.value)} placeholder="What deserves attention?" rows={8}/><div className="dialogActions"><span>{body.length}/30,000</span><button className="primary" disabled={!body.trim()} onClick={publish}>Publish</button></div></dialog></div>}
