"use client";
import {useRef,useState} from "react";import type {Notification} from "@/lib/types";import {Avatar} from "../Avatar";import {BellIcon,TrashIcon} from "../Icons";import styles from "./NotificationRow.module.css";
const time=(value:string)=>{const d=new Date(value),seconds=Math.floor((Date.now()-d.getTime())/1000);if(seconds<60)return `${Math.max(1,seconds)}m`;if(seconds<3600)return `${Math.floor(seconds/60)}m`;if(seconds<86400)return `${Math.floor(seconds/3600)}h`;if(seconds<172800)return "Yesterday";return d.toLocaleDateString(undefined,{month:"short",day:"numeric"})};
export function NotificationRow({item,onOpen,onDelete}:{item:Notification;onOpen:()=>void;onDelete:()=>void}){const[offset,setOffset]=useState(0),start=useRef({x:0,y:0}),mode=useRef<"pending"|"horizontal"|"vertical">("pending"),swiped=useRef(false),width=useRef(0);return <div className={styles.shell}><button className={`${styles.delete} ${styles.left}`} onClick={onDelete}><TrashIcon/>Delete</button><button className={`${styles.delete} ${styles.right}`} onClick={onDelete}><TrashIcon/>Delete</button><article className={`${styles.row} ${!item.read?styles.unread:""}`} style={{transform:`translateX(${offset}px)`}} onPointerDown={e=>{start.current={x:e.clientX,y:e.clientY};mode.current="pending";swiped.current=false;width.current=e.currentTarget.clientWidth;e.currentTarget.setPointerCapture(e.pointerId)}} onPointerMove={e=>{if(!e.currentTarget.hasPointerCapture(e.pointerId))return;const dx=e.clientX-start.current.x,dy=e.clientY-start.current.y;if(mode.current==="pending"&&Math.max(Math.abs(dx),Math.abs(dy))>7)mode.current=Math.abs(dx)>Math.abs(dy)+5?"horizontal":"vertical";if(mode.current==="horizontal"){swiped.current=true;setOffset(Math.max(-110,Math.min(110,dx)))}}} onPointerUp={e=>{if(e.currentTarget.hasPointerCapture(e.pointerId))e.currentTarget.releasePointerCapture(e.pointerId);const full=Math.abs(offset)>width.current*.4;if(full){onDelete();setOffset(0)}else setOffset(Math.abs(offset)>55?(offset<0?-92:92):0)}} onClick={()=>{if(swiped.current){swiped.current=false;return}if(offset)setOffset(0);else onOpen()}}><span className={styles.event}><BellIcon/></span>{item.actor?<Avatar user={item.actor} size={40}/>:<Avatar name="Jesca Social Work" variant="brand" size={40}/>}<div className={styles.body}><p><b>{item.actor?.display_name||"Jesca Social Work"}</b> {item.text}</p>{item.post&&<span>{item.post.preview}</span>}<small title={new Date(item.created_at).toLocaleString()}>{time(item.created_at)}</small></div>{!item.read?<i className={styles.dot} aria-label="Unread"/>:<button className={`iconButton ${styles.more}`} aria-label="Delete notification" onClick={e=>{e.stopPropagation();onDelete()}}><TrashIcon/></button>}</article></div>}
