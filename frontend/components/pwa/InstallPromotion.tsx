"use client";import styles from "./InstallPromotion.module.css";
export type InstallPromptEvent=Event&{prompt:()=>Promise<void>;userChoice:Promise<{outcome:"accepted"|"dismissed"}>};
export function InstallPromotion({prompt,ios,onClose,onInstalled}:{prompt:InstallPromptEvent|null;ios:boolean;onClose:()=>void;onInstalled:()=>void}){async function install(){if(!prompt)return;await prompt.prompt();const result=await prompt.userChoice;onClose();if(result.outcome==="accepted")onInstalled()}return <aside className={styles.card} aria-label="Install Jesca Social Work"><img src="/icon-192.png" alt=""/><div><b>Get the Jesca app</b><p>{ios?"Tap Share, then choose Add to Home Screen.":"Faster access and native notifications."}</p></div>{prompt&&<button className="primary" onClick={()=>void install()}>Install</button>}<button className={styles.close} aria-label="Close install promotion" onClick={onClose}>×</button></aside>}
