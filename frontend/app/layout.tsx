import type {Metadata,Viewport} from "next";import {Suspense} from "react";import "./globals.css";import {AppShell} from "@/components/AppShell";import {AppProvider} from "@/components/AppProvider";import {PwaManager} from "@/components/PwaManager";
export const metadata:Metadata={title:{default:"Jesca Social Work",template:"%s · Jesca Social Work"},description:"Jesca Social Work editorial community",applicationName:"Jesca Social Work",appleWebApp:{capable:true,statusBarStyle:"default",title:"Jesca Social Work"},icons:{apple:"/icon-192.png"}};
export const viewport:Viewport={width:"device-width",initialScale:1,viewportFit:"cover",themeColor:"#191c1d"};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body><Suspense fallback={null}><AppProvider><AppShell>{children}</AppShell><PwaManager/></AppProvider></Suspense></body></html>}
