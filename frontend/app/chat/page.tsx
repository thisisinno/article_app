"use client";import{useEffect,useRef,useState}from"react";import{api}from"@/lib/api";import{useApp}from"@/components/AppProvider";import styles from"./chat.module.css";import type{User}from"@/lib/types";
type Conversation={id:string;participants:User[];last_message:string;updated_at:string};type Message={id:string;client_id:string;body:string;sender:User;created_at:string};
export function websocketUrl(id:string){const scheme=window.location.protocol==="https:"?"wss:":"ws:";return `${scheme}//${window.location.host}/ws/chat/${id}/`}
export default function Chat(){const{user,authStatus,openAuth}=useApp();const[items,setItems]=useState<Conversation[]>([]),[active,setActive]=useState<Conversation|null>(null),[messages,setMessages]=useState<Message[]>([]),[body,setBody]=useState(""),[connection,setConnection]=useState("disconnected"),[error,setError]=useState("");const retry=useRef(0);useEffect(()=>{if(user)api<{results:Conversation[]}>("/conversations/").then(x=>{setItems(x.results);setActive(x.results[0]||null)}).catch(x=>setError((x as Error).message));else if(authStatus==="anonymous")openAuth()},[user,authStatus]);useEffect(()=>{if(!active)return;let stopped=false,ws:WebSocket|undefined,timer=0;api<{results:Message[]}>(`/conversations/${active.id}/messages/`).then(x=>setMessages(x.results)).catch(x=>setError((x as Error).message));const connect=()=>{if(stopped)return;setConnection("connecting");ws=new WebSocket(websocketUrl(active.id));ws.onopen=()=>{retry.current=0;setConnection("connected")};ws.onmessage=e=>{try{const m=JSON.parse(e.data);if(m.type==="message")setMessages(v=>v.some(x=>x.client_id===m.client_id)?v:[...v,m])}catch{setError("An invalid chat message was received.")}};ws.onerror=()=>setConnection("error");ws.onclose=()=>{if(stopped)return;setConnection("disconnected");if(retry.current<5)timer=window.setTimeout(connect,Math.min(1000*2**retry.current++,16000))}};connect();return()=>{stopped=true;clearTimeout(timer);ws?.close()}},[active]);async function send(){if(!active||!body.trim())return;const client_id=crypto.randomUUID(),text=body;setBody("");setMessages(v=>[...v,{id:client_id,client_id,body:text,sender:user!,created_at:new Date().toISOString()}]);try{await api(`/conversations/${active.id}/messages/`,{method:"POST",body:JSON.stringify({body:text,client_id})})}catch(x){setMessages(v=>v.filter(m=>m.client_id!==client_id));setError((x as Error).message)}}if(authStatus==="loading")return <div className="empty">Loading session…</div>;if(!user)return <div className="empty">Sign in to use private chat.</div>;return <div className={styles.chat}><section className={`${styles.list} ${active?styles.mobileHidden:""}`}><header><h1>Messages</h1><input placeholder="Search conversations"/></header>{items.map(c=><button className={active?.id===c.id?styles.selected:""} onClick={()=>setActive(c)} key={c.id}><span>{c.participants[0]?.display_name[0]}</span><b>{c.participants[0]?.display_name||"Conversation"}</b><small>{c.last_message||"Start a thoughtful conversation"}</small></button>)}</section><section className={`${styles.thread} ${!active?styles.mobileHidden:""}`}>{active?<><header><button onClick={()=>setActive(null)}>←</button><b>{active.participants[0]?.display_name}</b><span>{connection}</span></header>{error&&<p className="formError">{error}</p>}<div className={styles.messages}>{messages.map(m=><div key={m.client_id} className={m.sender.username===user.username?styles.mine:styles.theirs}>{m.body}<small>{new Date(m.created_at).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"})}</small></div>)}</div><footer><input value={body} onChange={e=>setBody(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="Type a message…"/><button className="primary" onClick={send}>Send</button></footer></>:<div className="empty">Select a conversation</div>}</section></div>}
